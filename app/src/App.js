function App() {
  return (
    <div style={styles.app}>
        Hello Product feedback app 👋
    </div>  
  );
}

const styles = {
  app: {
    height: '100vh',
    display: 'grid',
    placeItems: 'center'
  }
}

export default App;
